syntax = "v1"

info (
    desc:   "用户"
    author: "时叶zshey"
    email:  "z_shey@163.com"
)

import (
    "type/type.api"
)

@server(
    jwt: Auth
//    group: template
    prefix: /api/system
)
service main {
//    @handler register
//    post /register (Registerreq) returns (JwtTokenresp)

    @handler login
    post /login (Loginreq) returns (JwtTokenresp)
}
